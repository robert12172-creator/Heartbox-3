# HeartBox API (Node + Express + MongoDB)

## Local
```bash
cp .env.example .env
npm install
npm run dev
# -> http://localhost:10000/healthz
```

## Env
- `MONGODB_URI` — your MongoDB connection string (do not commit).
